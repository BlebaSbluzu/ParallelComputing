
void initialiseWorld(CELL **current, CELL **future,
                     unsigned long *ptr_to_numS, unsigned long *ptr_to_numZ){


 int rows = 10, cols = 10;

 srand(time(NULL));

 int z_row = rand() % rows;
    int z_col = rand() % cols;

    
    
}
